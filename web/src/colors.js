// Per-seat colours, shared by the board, move log, and player chips so a
// player's name and their pieces always match. Seat 0 = red, seat 1 = blue.
// Index 2 is a neutral colour (cream) for pieces owned by no player, e.g. the
// shared king in Borderline.
export const SEAT_FILL = ['#d23b3b', '#3b6fd2', '#e8dcc8']
export const SEAT_STROKE = ['#7a1414', '#173a7a', '#8a7a62']
